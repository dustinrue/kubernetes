apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "proxysql-cluster-aio.fullname" . }}-test-connection"
  labels:
    {{- include "proxysql-cluster-aio.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "proxysql-cluster-aio.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
